// Configuration for API endpoints and other global settings

// Change this to point to your backend server
export const API_URL = 'http://localhost:8080';

// Polling interval for fetching messages (in milliseconds)
export const MESSAGE_POLL_INTERVAL_IN_MILLISECONDS = 5000;

// Polling interval for health check (in milliseconds)
export const HEALTHCHECK_INTERVAL_IN_MILLISECONDS = 5000;
